# WP-007.Z.8 Security Review

## Status

Approved.

## Security boundary

Validation is current-identity, Windows-authenticated, encrypted, bounded and
read-only. It accepts no credentials or SQL Authentication and contains only
catalog SELECT queries. It performs no grant, schema change, migration,
retention deletion, credential exposure or authentication downgrade.

No individual approver identity is recorded or inferred.
